# zigttp v0.1.0 Beta - Pre-Release Checklist

Launch-readiness gate for the zigttp public beta. zigttp is a Zig-based
serverless JavaScript runtime for FaaS, powered by the `zigts` engine.

This document tracks whether the beta is ready to ship. It is a readiness gate,
not a release-mechanics runbook. The mechanical "cut the release" steps (test
gates, cross-compile verification, tagging, GitHub Release) live in
[`RELEASE_CHECKLIST.md`](../../RELEASE_CHECKLIST.md) at the repo root and are
not duplicated here.

## How to use this checklist

- Every item is a checkbox. Each carries a `Status:` note from a codebase
  survey (2026-05-22) describing what already exists, so a checked box means
  the work is genuinely done, not assumed.
- Status terms:
  - `EXISTS` - already implemented; this item is verify-only.
  - `PARTIAL` - exists but incomplete; finish it.
  - `GAP` - does not exist; create it.
  - `MEASURE` - a measurement or audit task with no fixed artifact.
- Any unchecked launch blocker must have a named owner and a disposition
  (ship / fix / defer) recorded before v0.1.0 ships. Use the Disposition
  column in the Launch blockers table at the bottom.

## Release metadata

- Release: `v0.1.0-beta`
- Release owner: ______
- Target date: ______
- Engine: `zigts` (Zig JavaScript engine; multi-tier JIT, generational GC)
- Supported platforms: macOS and Linux, x86-64 and ARM64

## 1. Performance & Benchmarking

- [ ] Measure cold starts from process launch to first handler response; beta gate: p50 and p95 under 5ms on target hardware.
  - Status: MEASURE. Documented cold start today is ~71ms optimized / ~83ms baseline on macOS (`docs/performance.md`); the ~3ms figure is runtime-init only, not full cold start. The 5ms gate is not currently met - plan optimization work or record a documented exception.
- [ ] Measure baseline memory before the first request; beta gate: under 5MB RSS, or a clearly documented exception.
  - Status: MEASURE. README claims a ~4MB baseline; this needs a real RSS measurement with the method stated.
- [ ] Add repeatable benchmark scripts for the `/health`, `/echo`, JSON-response, and error-response paths.
  - Status: PARTIAL. `zig build bench` (`packages/runtime/src/benchmark.zig`) covers 12 microbenchmarks including an HTTP handler; per-path scripts to be confirmed or added in the `zigttp-bench` sibling repo.
- [ ] Compare equivalent handlers against Node.js and Deno with identical payloads, concurrency, and hardware.
  - Status: PARTIAL. A Deno HTTP-throughput baseline exists in `docs/performance.md`; the Node.js comparison is missing.
- [ ] Stress test the `zigts` JIT under FaaS-style burst concurrency.
  - Status: PARTIAL. The JIT is a real multi-tier compiler (baseline + optimized, x86-64 and ARM64) and `benchmark.zig` already emits tier-promotion and deopt counters; a burst-concurrency harness is still needed.
- [ ] Stress test the generational GC with short-lived request objects and mixed payload sizes.
  - Status: GAP. The GC is generational (bump-allocated nursery + tenured mark-sweep with SIMD sweeping); a dedicated GC stress test does not exist.
- [ ] Record throughput, latency, cold start, warm start, RSS, allocator failures, and GC pause metrics.
  - Status: MEASURE.
- [ ] Publish benchmark methodology and raw results so landing-page claims are reproducible.
  - Status: GAP.

## 2. Documentation

- [ ] Complete the API reference for all `zigttp:*` virtual modules, including JWT (`zigttp:auth`), JSON Schema (`zigttp:validate`), and Crypto (`zigttp:crypto`).
  - Status: EXISTS. All 23 virtual modules already have API docs under `docs/virtual-modules/`; this item is a completeness review, not creation.
- [ ] Document request and response types, lifecycle, error handling, and module import examples.
  - Status: PARTIAL. Covered across `docs/user-guide.md` and `docs/virtual-modules/README.md`.
- [ ] Write and verify the Getting Started guide: CLI install, `zigttp dev`, `zigttp test`, `zigttp build`.
  - Status: EXISTS. `docs/getting-started.md` exists; verify it covers `build`.
- [ ] Document the exact ES5/ES6 subset supported by `zigts`.
  - Status: EXISTS. `docs/language-subset.md`.
- [ ] Document unsupported JavaScript and TypeScript features with examples.
  - Status: EXISTS. `docs/feature-detection.md` covers 30+ features with suggested alternatives.
- [ ] Add deployment guides for AWS Lambda and Cloudflare Workers.
  - Status: GAP. No Lambda or Cloudflare Workers adapters exist today; the beta deploy path is `zigttp deploy` plus the edge runtime. Either write conceptual guides or defer with a named owner (see Launch blockers).
- [ ] Add troubleshooting notes for syntax errors, unsupported runtime APIs, and local dev issues.
  - Status: PARTIAL. `docs/user-guide.md` has a troubleshooting section.

## 3. Security & Reliability

- [ ] Audit request isolation across pooled handler executions.
  - Status: EXISTS. A HandlerPool with arena-escape audit counters exists (`packages/runtime/src/runtime_pool.zig`); this item is the audit itself.
- [ ] Add regression tests proving no headers, body data, locals, or module state leak across requests unless explicitly shared.
  - Status: GAP. No explicit data-leakage test exists.
- [ ] Verify Zig VM memory safety around bytecode execution, stack bounds, allocator lifetimes, and error paths.
  - Status: EXISTS. Bounds checks are in place - the checked value-stack push returns `StackOverflow`, with 1024-deep call and state-stack caps (`packages/zigts/src/context.zig`).
- [ ] Run sanitizer or equivalent debug builds where available.
  - Status: PARTIAL. The Zig Debug build carries safety checks; run an explicit pass and add a stack-overflow test (listed under "Missing Tests" in `TODO.md`).
- [ ] Enforce server payload size limits with documented defaults.
  - Status: EXISTS. `server.zig` enforces a 1MB `max_body_size` default, configurable and tested; the limit is currently undocumented in user-facing docs - document it.
- [ ] Add basic rate limiting, or explicitly document why it is deferred from v0.1.0.
  - Status: PARTIAL. The `zigttp:ratelimit` virtual module exists (handler-level); a server-level per-IP or global gate is a gap.
- [ ] Redact secrets and request bodies from default logs.
  - Status: MEASURE. Needs an audit of default logging.
- [ ] Define crash behavior, panic handling, and non-zero exit expectations.
  - Status: GAP. Needs a written spec.

## 4. Developer Experience & CLI

- [ ] Ensure JS/TS syntax errors include file, line, column, snippet, and an actionable message.
  - Status: EXISTS. Structured diagnostics with error codes ZTS0xx-ZTS6xx, source locations, and suggestion fields exist (`packages/tools/src/json_diagnostics.zig`); verify the human-facing (non-JSON) rendering is equally clear.
- [ ] Test TypeScript/TSX stripping across flat files, nested folders, path imports, JSX/TSX syntax, and unsupported syntax.
  - Status: PARTIAL. `packages/zigts/src/stripper.zig` has 77 unit tests; a known gap is that enums, namespaces, and decorators are hard-rejected (`TODO.md`) - confirm the error is actionable or document the limitation.
- [ ] Finalize the `zigttp build` output structure and documented artifacts.
  - Status: EXISTS. The `build` command exists; this item finalizes and documents its output.
- [ ] Finalize `zigttp dev` hot-reload behavior for handler edits, config edits, and syntax failures.
  - Status: EXISTS. `packages/runtime/src/live_reload.zig` exists (`dev` implies `--watch --prove`).
- [ ] Extend `zigttp doctor` with checks for a missing entry file, an invalid `zigttp.json`, an unsupported platform, and missing permissions.
  - Status: PARTIAL. The `doctor` command already exists; this extends its checks.
- [ ] Confirm the CLI exits with stable non-zero codes for check, build, and test failures.
  - Status: MEASURE. Needs verification.

## 5. Website & Marketing

- [ ] Finalize the landing page value proposition: Zig-based serverless JavaScript runtime, fast cold starts, small memory footprint.
  - Status: EXISTS. `zigttp-website/static/index.html` is a complete landing page with JSON-LD and Open Graph / Twitter Card metadata.
- [ ] Add a live or reproducible benchmark comparison against Node.js and Deno.
  - Status: GAP.
- [ ] Verify the "Try it in the browser" playground works for beta and is linked from the landing page.
  - Status: EXISTS. The WASM analyzer playground ships (`playground.js` plus the compiled `zigts-analyzer.*.wasm`).
- [ ] Host searchable documentation.
  - Status: GAP. Docs live in the main repo and are not embedded or searchable on the site. Decide: embed docs with search, or prominently surface the external docs URL.
- [ ] Confirm the GitHub README has a quick start, examples, a benchmark summary, limitations, a roadmap, and contribution links.
  - Status: EXISTS. `README.md` is substantial (1,354 lines); confirm every section is present, especially the roadmap.
- [ ] Ensure all public performance claims link to methodology and the commit or version they were measured on.
  - Status: GAP.

## 6. Release Engineering

- [ ] Add pull-request and push CI for format, check, tests, and release-artifact validation.
  - Status: GAP. `.github/workflows/release.yml` exists but triggers only on `tags: v*`, so pull requests currently get no automated testing.
- [ ] Confirm cross-compilation jobs for Linux and macOS, x86-64 and ARM64.
  - Status: EXISTS. `release.yml` builds all four targets; `build.zig` supports `-Dtarget` and SQLite is vendored as a static amalgamation.
- [ ] Produce signed or checksummed binary releases.
  - Status: EXISTS. SHA-256 checksums are generated in `release.yml` and verified by `install.sh`.
- [ ] Add a release-notes template for the v0.1.0 beta.
  - Status: GAP.
- [ ] Confirm the open-source license.
  - Status: EXISTS. MIT `LICENSE` at the repo root.
- [ ] Confirm contribution guidelines and the security disclosure policy; add a code of conduct and issue/PR templates.
  - Status: PARTIAL. `CONTRIBUTING.md` and `SECURITY.md` exist; `CODE_OF_CONDUCT.md`, `.github/ISSUE_TEMPLATE/`, and `.github/pull_request_template.md` are gaps.
- [ ] Confirm no secrets, generated artifacts, or local build outputs are committed.
  - Status: MEASURE. Audit task.
- [x] Reconcile the version string. The release is named `v0.1.0-beta`, but `packages/zigts/src/root.zig` reports `1.0.0` and `build.zig.zon` `.version` is `0.16.0`. Pick the canonical source and align all three (and point `RELEASE_CHECKLIST.md` at it).
  - Status: DONE. `build.zig.zon` `.version` is the canonical source, now `0.1.0-beta`. `packages/zigts/src/root.zig` `version` struct aligned (major 0, minor 1, patch 0, string `0.1.0-beta`). `RELEASE_CHECKLIST.md` already derives the version from `build.zig.zon` line 3. Note: the sub-package manifests `packages/zigts/build.zig.zon` and `packages/runtime/build.zig.zon` still carry `.version = "0.16.0"`, and `openapi_manifest.zig`/`expert_meta.zig` default to `"0.16.0"` - left as-is since they were out of this item's named scope.

## Launch blockers

Record every unchecked item the team treats as a hard blocker. Each needs an
owner and a disposition (ship as known issue / fix before launch / defer
post-beta) before v0.1.0 ships.

| Item | Domain | Owner | Disposition |
|------|--------|-------|-------------|
|      |        |       |             |
