.zig-cache/
zig-out/
zig-pkg/
.DS_Store

# Generated files (build-time precompilation)
packages/runtime/generated/

# local scratch / experiments (should not be committed)
size_check
test_cc
test_mmap
test_mmap2
test_mprotect
test_prot
test_time
build_link.log
.codex/skills/
proof-report.txt
proof.json
codedb.snapshot

# Ephemeral 16-char scratch project dirs spawned at repo root by some
# tool/agent workflow. Contents are single-line handler.ts stubs; never
# user work. No legit top-level dir in this repo is 16 chars long, so
# this glob is safe.
/????????????????/
