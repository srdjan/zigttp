---
name: add-env
description: "Add an env var to the contract and handler. Usage: /template:add-env <VAR_NAME>"
---
Add the environment variable {{1}} to the handler. Import it via `zigttp:env`, add it to the env contract section, and use it where needed. Run /check after the edit.
